#!/usr/bin/env bash
set -euo pipefail

# Resolve repository root relative to this script and delegate to the Node entrypoint.
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "${ROOT_DIR}/js/terrible.js" "$@"
