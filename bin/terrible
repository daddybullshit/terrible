#!/usr/bin/env bash
set -euo pipefail

# Locate repo root relative to this script and delegate to scripts/terrible.sh
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${ROOT_DIR}/scripts/terrible.sh" "$@"
