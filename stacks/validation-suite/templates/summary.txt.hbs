Validation demo summary
=======================

Stack objects ({{length (values global.objects)}} total):
{{#each (sort_by (values global.objects) "id")}}
- {{id}} (class: {{class}}, status: {{default properties.status status}})
{{/each}}

Generated by Terrible. Use `--warnings-as-errors` to make schema warnings fatal and `--warn-extra-fields` to flag fields not declared in schemas.
